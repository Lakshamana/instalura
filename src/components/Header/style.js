import { StyleSheet } from 'react-native'

const style = StyleSheet.create({
  userPhoto: {
    width: 50,
    height: 50,
    margin: 10,
    borderRadius: 50
  },
  header: {
    flexDirection: 'row',
    alignItems: 'center'
  }
})

export default style
