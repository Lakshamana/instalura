import { StyleSheet } from 'react-native'

const style = StyleSheet.create({
  inline: {
    flexDirection: 'row',
    alignItems: 'center'
  },
  send: {
    width: 30,
    height: 30,
    margin: 5
  },
  input: {
    flexGrow: 1,
    margin: 5
  }
})

export default style
